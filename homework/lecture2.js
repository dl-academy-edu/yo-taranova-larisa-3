let userNumber = prompt();

for ( let i = 1; i <= userNumber; i++) {
    if (i % 4 === 0) continue;
    console.log (i)
}

let i = 1;

while ( userNumber ) {
    if ( i === 
        userNumber) 
    break;
    console.log (userNumber)
    i++
}